"use strict";

let str1;
let phrase;
let statement = true;
str1 = "word";

// Repeat function can be used to repeat strings
alert(str1.repeat(5));

// Backticks can be use to embed variables into strings
alert(`This right here --> ${str1} <-- is embedded`);

alert(typeof(str1));

alert(`hello ${1}`);